//! Canvas interaction/rendering boundary.
//!
//! The large drawing functions currently live in `ui::app` because this pass
//! only moves the app shell out of `main.rs`. Canvas code should migrate here
//! in small compile-safe steps.
